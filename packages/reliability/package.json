{
  "name": "@stack-hq/reliability",
  "version": "1.0.0",
  "description": "Circuit breaker, rate limiting (token bucket, fixed window, sliding log, sliding window counter), retry with backoff, and bulkhead isolation.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "prepublishOnly": "npm run build"
  },
  "files": ["dist"],
  "keywords": ["stack-hq", "reliability", "backend"],
  "author": "Abhishek Awari",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/abhishekawari3/stack-hq.git",
    "directory": "packages/reliability"
  },
  "publishConfig": {
    "access": "public"
  },
  "engines": { "node": ">=16" },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "typescript": "^5.4.5"
  }
}
